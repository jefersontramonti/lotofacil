<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <!-- RF-07.7: pedida em tempo de execução só quando o usuário liga o
         primeiro aviso, nunca declarada como bastante por si (ver TelaPerfil). -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:name=".TrevoApplication"
        android:label="@string/app_name"
        android:theme="@style/Theme.Trevo"
        android:usesCleartextTraffic="false">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Inicialização sob demanda do WorkManager: TrevoApplication provê
             a Configuration com HiltWorkerFactory (necessária para @HiltWorker),
             então o initializer automático via androidx.startup precisa ser
             removido daqui. -->
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">
            <meta-data
                android:name="androidx.work.WorkManagerInitializer"
                android:value="androidx.startup"
                tools:node="remove" />
        </provider>

    </application>

</manifest>
