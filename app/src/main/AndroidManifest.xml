<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <!-- RF-07.7: pedida em tempo de execução só quando o usuário liga o
         primeiro aviso, nunca declarada como bastante por si (ver TelaPerfil). -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:name=".TrevoApplication"
        android:label="@string/app_name"
        android:theme="@style/Theme.Trevo"
        android:usesCleartextTraffic="false">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Inicialização sob demanda do WorkManager: TrevoApplication provê
             a Configuration com HiltWorkerFactory (necessária para @HiltWorker),
             então o initializer automático via androidx.startup precisa ser
             removido daqui. -->
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">
            <meta-data
                android:name="androidx.work.WorkManagerInitializer"
                android:value="androidx.startup"
                tools:node="remove" />
        </provider>

        <!-- RF-09.2/09.3: App ID de TESTE do Google (funciona sem conta
             AdMob). Trocar pelo App ID real do AdMob antes de publicar —
             ver AnuncioRecompensadoManager para o ad unit id equivalente. -->
        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="ca-app-pub-3940256099942544~3347511713" />

        <!-- RF-08.3: o PDF exportado vai pro cache do app; FileProvider dá
             uma content:// URI pro app de destino (visualizador de PDF,
             WhatsApp etc.) ler sem expor um caminho file:// direto. -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>

</manifest>
